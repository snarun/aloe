/**
 * Configurable Application Settings.
 * Application settings should be injected to the angular application while bootstrapping.
 *
 * @export
 * @class AppSettings
 */
export declare class AppSettings {
    cookieName: string;
    headerName: string;
    cacheExpiry: number;
    serviceURI: string;
    notificationURI: string;
}
